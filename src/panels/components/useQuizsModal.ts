

export const useQuizsModal = () => {
// //   // generateQuizHandler.mutate({ noteId: noteId })

  // //   console.log('noteId',noteId)
  
  // //   useEffect(() => {
  // //     if (generateQuizHandler.isLoading || generateQuizHandler.isSuccess ) return
  // //     // generateQuizHandler.mutate({ noteId: noteId })
  // //     console.log('Quiz generated')
  // //   }, [])
  
  
// //   return {
// //     generateQuizHandler
// //   }
}