// The key whithin the browser's local storage which contains the
// JSON Web Token used in calls to the API.
export const LS_AUTH_TOKEN_KEY = 'token'

// The key whithin the browser's local storage which contains the
// current group's ID.
export const LS_GROUP_ID_KEY = 'group_id'
