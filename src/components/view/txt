<Link
              to={'/'}
              className={`flex items-center justify-between rounded-md p-3 hover:bg-gray-100 ${
                currentPath === '/' ? 'bg-gray-100' : ''
              }`}
            >
              <div className='flex items-center'>
                <Square2StackIcon className='h-5 w-5 text-gray-400' />
                <span className='ml-2 font-medium text-gray-600'>
                  {formatMessage({ id: 'GENERIC.home' })}
                </span>
              </div>
              {0 > 0 && (
                <span className='flex h-5 w-5 items-center justify-center rounded-full bg-purple-200 p-1 text-lg font-medium text-purple-700'>
                  {0}
                </span>
              )}
            </Link>
            {/* Groups */}
            <Link
              to={'/groups'}
              className={`flex items-center justify-between rounded-md p-3 hover:bg-gray-100 ${
                currentPath === '/groups' ? 'bg-gray-100' : ''
              }`}
            >
              <div className='flex items-center'>
                <Square2StackIcon className='h-5 w-5 text-gray-400' />
                <span className='ml-2 font-medium text-gray-600'>
                  {formatMessage({ id: 'GENERIC.groups' })}
                </span>
              </div>                
            </Link>
            {/* Notes */}
            <Link
              to='/notes'
              className={`flex items-center justify-between rounded-md p-3 hover:bg-gray-100 ${
                currentPath === '/notes' ? 'bg-gray-100' : ''
              }`}
            >
              <div className='flex items-center'>
                <Square2StackIcon className='h-5 w-5 text-gray-400' />
                <span className='ml-2 font-medium text-gray-600'>
                  {formatMessage({ id: 'GENERIC.notes' })}
                </span>
              </div>
            </Link>
            {/* Profil */}
            <Link
              to={'/profile'}
              className={`flex items-center justify-between rounded-md p-3 hover:bg-gray-100 ${
                currentPath === '/profil' ? 'bg-gray-100' : ''
              }`}
            >
              <div className='flex items-center'>
                <UserIcon className='h-5 w-5 text-gray-400' />
                <span className='ml-2 font-medium text-gray-600'>
                  {formatMessage({ id: 'GENERIC.profile' })}
                </span>
              </div>
              {(listInvitesQ.isSuccess && listInvitesQ.data.invites ? listInvitesQ.data.invites.length : 0) > 0 && (
                <span className='flex h-5 w-5 items-center justify-center rounded-full bg-purple-200 p-1 text-lg font-medium text-purple-700'>
                  {0}
                </span>
              )}
            </Link>